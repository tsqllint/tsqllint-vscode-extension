language: node_js

node_js:
- 'node'

os:
- linux

install:
- npm install

script:
- npm run lint
- vsce package

deploy:
  provider: releases
  api_key: $GithubToken
  file_glob: true
  file: /**\.vsix/
  skip_cleanup: true
  on:
    tags: true